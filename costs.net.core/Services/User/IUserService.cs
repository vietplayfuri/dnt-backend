namespace dnt.core.Services.User
{
    using System;
    using System.Collections.Generic;
    using System.Threading.Tasks;
    using Models;
    using Models.Response;
    using Models.User;

    public interface IUserService
    {
        //Task<ICostUserModel> Get(Guid id, UserViewMode? viewMode, BuType buType);

        //Task<IEnumerable<ICostUserModel>> Get(UserQuery query, UserViewMode? viewMode, BuType buType);

        //Task<ICostUserModel> Get(string gdamUserId, UserViewMode? viewMode, BuType buType);

        //Task<OperationResponse> Update(UserIdentity userIdentity, Guid id, UpdateUserModel model, BuType buType);

        //Task<CostUserModel> Get(string email, BuType buType);

        //Task AddUserToDb(GdamUser gdamUser);

        //Task UpdateSessionData(Sessiondata sessionData, string gdamUserId);

        //Task<bool> CanUserAddOwners(Guid costId, UserIdentity user);

        //Task UpdateUsersAcl();
    }
}
